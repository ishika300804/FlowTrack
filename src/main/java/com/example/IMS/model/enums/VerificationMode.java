package com.example.IMS.model.enums;

/**
 * Verification execution mode
 */
public enum VerificationMode {
    MOCK,     // Use mock responses (for testing/development)
    REAL      // Use actual external API calls
}
